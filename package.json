{
  "name": "@3fv/guard",
  "version": "1.4.6",
  "description": "TypeScript guard suite",
  "main": "index.js",
  "types": "index.d.ts",
  "resolutions": {
    "lodash": "^4.17.15",
    "lodash.template": "^4.5.0"
  },
  "scripts": {
    "build": "./scripts/build.sh",
    "build:watch": "yarn run build -- --watch",
    "test": "jest",
    "test:watch": "jest --watch",
    "preversion": "yarn build && yarn test",
    "//postversion": "git push --tags && yarn publish . --tag $npm_package_version --new-version $npm_package_version && git push && echo \"Successfully released version $npm_package_version!\"",
    "postversion": "./scripts/release.sh $npm_package_version && echo \"Successfully released version $npm_package_version!\"",
    "//release": "yarn build && yarn test && yarn version --patch --non-interactive"
  },
  "publishConfig": {
    "access": "public"
  },
  "files": [
    "**/*.d.ts",
    "**/*.js",
    "**/*.map",
    "package.json",
    "README.md"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/3fv/guard.git"
  },
  "keywords": [
    "typescript",
    "guard",
    "typeguard",
    "typesafe",
    "type",
    "check"
  ],
  "author": "Jonathan Glanz",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/3fv/guard/issues"
  },
  "homepage": "https://github.com/3fv/guard#readme",
  "dependencies": {
    "lodash": "^4.17.15"
  },
  "devDependencies": {
    "@types/jest": "^24.0.23",
    "@types/lodash": "^4.14.149",
    "@types/node": "^12.12.17",
    "jest": "^24.2.0",
    "npm-run-all": "^4.1.5",
    "ts-jest": "^24.2.0",
    "typescript": "^3.7.3"
  }
}
